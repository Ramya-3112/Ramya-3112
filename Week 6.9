# Read input
n = int(input().strip())  # Array length
arr = []

# Read elements of the array
for _ in range(n):
    arr.append(int(input().strip()))

# Find distinct elements
distinct_elements = set(arr)

# Print distinct elements
print(*distinct_elements)
