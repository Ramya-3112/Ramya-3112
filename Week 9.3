def is_abundant(number):
   
    divisor_sum = 0
    for i in range(1, number):
        if number % i == 0:
            divisor_sum += i

    return divisor_sum > number

def abundant(number):
    if is_abundant(number):
        return "Yes"
    else:
        return "No"
    
